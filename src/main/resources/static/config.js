// Deja este valor vacio en desarrollo local.
// Cuando el frontend se publique en Hostinger y el backend quede en otro dominio,
// reemplaza el texto vacio por la URL base del backend, por ejemplo:
// window.SMGT_API_BASE_URL = "https://smgt-api.onrender.com";
window.SMGT_API_BASE_URL = "";
